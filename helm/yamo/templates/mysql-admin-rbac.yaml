apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "yamo.fullname" . }}-mysql-admin-role
  labels:
  {{- include "yamo.labels" . | nindent 4 }}
rules:
- apiGroups:
  - database.elnino.tech
  resources:
  - mysqls
  verbs:
  - '*'
- apiGroups:
  - database.elnino.tech
  resources:
  - mysqls/status
  verbs:
  - get

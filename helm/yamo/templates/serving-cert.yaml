apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "yamo.fullname" . }}-serving-cert
  labels:
  {{- include "yamo.labels" . | nindent 4 }}
spec:
  dnsNames:
  - '{{ include "yamo.fullname" . }}-webhook-service.{{ .Release.Namespace }}.svc'
  - '{{ include "yamo.fullname" . }}-webhook-service.{{ .Release.Namespace }}.svc.{{
    .Values.kubernetesClusterDomain }}'
  issuerRef:
    group: cert-manager.io
    kind: Issuer
    name: '{{ include "yamo.fullname" . }}-selfsigned-issuer'
  secretName: webhook-server-cert
